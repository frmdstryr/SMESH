diff -ru a/src/Controls/SMESH_Controls.cxx b/src/Controls/SMESH_Controls.cxx
--- a/src/Controls/SMESH_Controls.cxx
+++ b/src/Controls/SMESH_Controls.cxx
@@ -4358,6 +4358,8 @@
   void UnsetFlag( int flag ) { myFlags &= ~flag; }
   void GetParams( double & u, double & v ) const { u = myU; v = myV; }

+  Classifier(Classifier&& other) noexcept {mySolidClfr = 0; myFlags = 0;}
+
 private:
   bool isOutOfSolid (const gp_Pnt& p);
   bool isOutOfBox   (const gp_Pnt& p);
